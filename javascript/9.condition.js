// if and else statement
//  == and != (only value compare not type)
//  === and !== (value and type both get compare)
//  && and || 
// ternary operator-> condition ? true : false

let age = 12;

if(typeof age == 'number')
{
    console.log(age)
}
else 
{
    console.log('not a number')
}

// switch
// break and continue
/*
switch(var):
    case a : sdkj
    break
    .
    .
    .
    .
    .
    default: dsjkskd
    break
*/